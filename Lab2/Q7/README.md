## Question 7: multi-threaded histogram
This program creates a group of threads to parallelize building the histogram of an array. It uses 30 bins for the histogram. The number of threads and size of the array are determined by the user at runtime.

To compile:

```make```

To run:

```./histogram T N```

where T is the number of threads and N is the length of the array
