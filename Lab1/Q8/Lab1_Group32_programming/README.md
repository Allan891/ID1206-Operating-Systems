## Question 8: Process creation and termination.

This program creates an array of N random numbers whose values are in the range of [0,1]. N is passed by the user via function arguments. It will then print the sum and the time (in seconds) that it took to perform the calculation

To compile:

```make```

To run:

```./group32_Q8 N```

where N is the length the array should be
